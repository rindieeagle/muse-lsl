/**
 * @krisspy-file
 * @type page
 * @name "DemoModules"
 * @title "Demo Modules"
 * @description "Limited preview of available training modules with sample content"
 * @routes ["/demo-modules"]
 * @flowName "onboarding"
 */

export default function DemoModules() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-purple-900 via-blue-900 to-indigo-900">
      {/* Content will be implemented in future task */}
    </div>
  );
}
